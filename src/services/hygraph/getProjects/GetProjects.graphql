query GetProjects($limit: Int, $skip: Int) {
  projectsConnection {
    aggregate {
      count
    }
  }
  projects(first: $limit, skip: $skip) {
    id
    slug
    name
    description
    date
    tags
    image {
      url
    }
  }
}
