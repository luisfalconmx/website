query GetBlogPost($hostname: String!, $slug: String!) {
  publication(host: $hostname) {
    post(slug: $slug) {
      title
      coverImage {
        url
      }
      content {
        text
      }
    }
  }
}
