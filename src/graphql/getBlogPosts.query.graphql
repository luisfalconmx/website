query getBlogPosts {
  publication(host: "luisfalconmx.hashnode.dev") {
    posts(first: 10) {
      edges {
        node {
          id
          title
          url
          slug
          coverImage {
            url
          }
        }
      }
    }
  }
}
